<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!--
        Allow HTTP traffic to the locally hosted Flask/Ollama server used via
        adb reverse, typically through 127.0.0.1:5000 on the Android device.
    -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
